### Script used to find process which is occupied by a specific port

Mostly because I like the command but can't seem to remember it...

Install: `brew tap johanlewenhaupt/portprocess && brew install portprocess`
